{
  "name": "omnik-freshdesk-webhook",
  "version": "1.0.0",
  "description": "Freshdesk webhook — indexes resolved tickets into Supabase for RAG",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.0",
    "express": "^4.19.0"
  }
}
